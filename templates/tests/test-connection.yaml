apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "school-cluster.fullname" . }}-test-connection"
  labels:
{{ include "school-cluster.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "school-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
